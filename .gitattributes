# Normalize line endings to LF in the repo (prevents CRLF noise on macOS/Windows)
* text=auto eol=lf

# Treat these as binary (no line-ending or diff munging)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp4 binary
*.woff2 binary
*.pdf binary
